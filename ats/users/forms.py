"""
Placeholder forms for the `users` app.

Removed third-party (allauth) dependencies to keep the project API-only.
Implement your own forms here as required (e.g., DRF serializers, Django
forms, or forms for admin use).
"""

from django import forms


class PlaceholderForm(forms.Form):
    """A tiny placeholder form so imports succeed while you implement real ones."""
    pass
